#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nativescript-angular/bin/update-app-ng-deps" "$@"
  ret=$?
else 
  node  "$basedir/../nativescript-angular/bin/update-app-ng-deps" "$@"
  ret=$?
fi
exit $ret
